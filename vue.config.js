/* const proxy = require('https-proxy-middleware');

module.exports = function (app) {
    app.use(proxy('/api', {
        target: 'https://localhost:44376/', // Cambia esta URL a la URL de tu backend
        logLevel: 'debug',
        changeOrigin: true
    }));
}; */
